Open bash window inside desired project

git init
git checkout --orphan <NEWBRANCH>

Copy the pre-made .gitignore or make another to only commit the src folder
Check 'git status' to ensure correct files

git add .
git commit -m 'COMMIT MESSAGE'

git remote add origin https://github.com/<PROJECT>.git
git push -u origin <NEWBRANCH>

<NEWBRANCH> = project name, eg. 'HW02'
<PROJECT> = Repo name, in this case 'Sgordon4/ComS228'


To pull into new project, 
git clone --single-branch -b <NEWBRANCH> https://github.com/<PROJECT>.git
